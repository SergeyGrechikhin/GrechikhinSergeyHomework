package holidayWork.generic.task2;

import java.util.ArrayList;
import java.util.Arrays;

public class PrintListDemo {
    public static void main(String[] args) {
        PrintList.printer(Arrays.asList(1, 2));
        PrintList.printer(Arrays.asList("раз", "два"));
        PrintList.printer(Arrays.asList(1.8,2.5));
    }
}
